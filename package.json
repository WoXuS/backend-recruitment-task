{
  "name": "fullstack-recruitment-task",
  "version": "1.0.0",
  "description": "----",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --watch assets/css/ -o assets/css/",
    "serve": "browser-sync start --proxy localhost:8000 --files 'assets/css/*.css, **/*.html, **/*.php'",
    "start": "concurrently \"npm:scss\" \"npm:serve\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "concurrently": "^8.2.0",
    "node-sass": "^9.0.0",
    "sass": "^1.63.2"
  }
}
